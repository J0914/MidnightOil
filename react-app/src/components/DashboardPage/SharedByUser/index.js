import React from 'react';

import styles from '../../../css-modules/dashboard.module.css';

const SharedByUser = () => {
    return (
        <div id={styles.user_share__wrapper}>
            <h2>Shared by User</h2>
        </div>
    );
}

export default SharedByUser;